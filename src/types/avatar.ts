export type ArmCell = {
  x: number;
  y: number;
  col: number;
  colDelta: number;
  colDeltaInt: number;
  row: number;
};

export type BodyCell = { x: number; y: number; row: number; col: number };

