export const CAMERA_DISTANCE = 9;
export const TARGET_HEIGHT_RATIO = 0.6;
export const CAMERA_FOV_DEG = 60;
export const CAMERA_NEAR = 0.1;
export const CAMERA_FAR = 1000;

// Render layers
// 0: default scene
// 1: player avatar (toggle per camera to hide in first-person while keeping mirrors visible)
export const PLAYER_LAYER = 1;
